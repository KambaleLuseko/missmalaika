enum EnumActions { VIEW, UPDATE, DELETE, SAVE, CANCEL, REFUND }

enum ModelNamesEnum {
  analyseTools,
  analyse,
  dueAnalyses,
  performedAnalyses,
  sample,
  transaction,
  transactionDetails,
  allTransactions,
  partner,
  store,
  user,
  wallet,
  payment,
  transfer,
  currency,
  permission
}

enum MessageType { error, warning, success, info }

enum TransactionTypesEnum { payment, recharge, transfer }

enum ConnectedUser { admin, guest }

enum TransactionUser { sender, receiver }
